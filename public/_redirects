# API routes are handled by Cloudflare Pages Functions (see functions/api/[[path]].ts)
# No proxy redirects needed here - Cloudflare Pages doesn't support external URL proxies

# SPA fallback for client-side routing
# Note: /api/* paths are excluded by _routes.json and handled by Functions
/*.html  /:splat  301
/index.html  /  301
