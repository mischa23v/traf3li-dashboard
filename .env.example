# Clerk Authentication
VITE_CLERK_PUBLISHABLE_KEY=

# Backend API URL (base domain only, versioning handled automatically)
# ⚠️ IMPORTANT: Do NOT include /api or /v1 suffix
VITE_API_URL=https://api.traf3li.com

# WebSocket URL for real-time features
VITE_WS_URL=wss://api.traf3li.com

# Sentry Error Tracking (optional)
# Get your DSN from https://sentry.io/
VITE_SENTRY_DSN=

# AI Worker URL for task suggestions
# Cloudflare Workers AI endpoint
VITE_AI_WORKER_URL=https://shrill-band-bfcb.mischa23v.workers.dev

# NOTE: CAPTCHA configuration is managed by the backend via /auth/captcha/settings
# The frontend fetches CAPTCHA settings from backend and renders accordingly
# No frontend CAPTCHA env variables needed - backend controls everything