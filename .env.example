# Clerk Authentication
VITE_CLERK_PUBLISHABLE_KEY=

# Backend API URL (base domain only, versioning handled automatically)
# ⚠️ IMPORTANT: Do NOT include /api or /v1 suffix
# For Vercel deployment (uses proxy): https://api.traf3li.com
# For AWS EC2 deployment: https://api-aws.traf3li.com
VITE_API_URL=https://api.traf3li.com

# WebSocket URL for real-time features
# For Vercel deployment: wss://api.traf3li.com
# For AWS EC2 deployment: wss://api-aws.traf3li.com
VITE_WS_URL=wss://api.traf3li.com

# Disable API proxy (set to true for non-Vercel deployments like AWS EC2)
# When true, frontend makes direct API calls instead of using Vercel rewrites
VITE_DISABLE_API_PROXY=false

# Sentry Error Tracking (optional)
# Get your DSN from https://sentry.io/
VITE_SENTRY_DSN=